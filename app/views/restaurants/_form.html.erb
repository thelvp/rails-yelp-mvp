<%= simple_form_for(@restaurant) do |f| %>
  <%= f.input :name %>
  <%= f.input :address %>
  <%= f.input :phone_number %>
  <%= f.input :category.downcase, as: :select, collection: Restaurant::CATEGORIES %>
  <%= f.submit %>
 <% end %>
